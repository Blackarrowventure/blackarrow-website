{
  "_note": "Black Arrow 3D catalog. Empty by owner instruction (2026-09-15) — products are added one at a time, only after explicit approval. Schema below (in _schema) documents every field the storefront can render; leave any field out of a real product and the UI degrades gracefully.",
  "_schema": {
    "id": "unique slug, e.g. bambu-lab-a1",
    "name": "string",
    "brand": "string, e.g. Bambu Lab / Creality / Elegoo / Flashforge / Anycubic / Snapmaker / Polymaker / eSUN / Sunlu",
    "category": "3D Printers | Filament | Accessories",
    "sku": "string, optional",
    "price": "number, SAR, excluding VAT",
    "salePrice": "number, optional — present only when the item is on sale",
    "vatIncluded": "boolean — whether price already includes 15% VAT",
    "currency": "SAR",
    "available": "boolean",
    "preorder": "boolean, optional",
    "stockQty": "number, optional",
    "featured": "boolean, optional",
    "onSale": "boolean, optional",
    "topPick": "boolean, optional — powers the shop's 'Top quality' quick filter",
    "images": "array of image paths — first is primary",
    "shortDesc": "string",
    "description": "string",
    "specs": "array of [label, value] pairs",
    "compatibility": "array of strings, optional",
    "warranty": "string, optional",
    "seoTitle": "string, optional",
    "seoDescription": "string, optional"
  },
  "products": []
}
