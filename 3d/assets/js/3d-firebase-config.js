/* Fill this in once you create a free Firebase project (console.firebase.google.com):
   Project settings -> General -> Your apps -> Web app -> copy the config object here.
   Until then, the account page shows a clear "not connected yet" state instead of
   a broken form -- nothing here talks to payments or a CRM. */
window.BLACK_ARROW_FIREBASE_CONFIG = null;
